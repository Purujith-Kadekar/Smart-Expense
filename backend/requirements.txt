# Backend — Flask API dependencies.
#
# OCR packages are installed separately in backend/Dockerfile, gated on the
# OCR_ENGINE build arg, because EasyOCR drags in PyTorch (~800 MB) and you
# should not pay that cost when running the test suite locally.
Flask==3.0.3
flask-cors==4.0.1
boto3==1.34.142
botocore==1.34.142
python-dotenv==1.0.1
gunicorn==22.0.0
bcrypt==4.1.3
PyJWT==2.9.0
