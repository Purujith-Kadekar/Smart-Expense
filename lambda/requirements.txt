# Ingestion Lambda — runtime dependencies.
#
# OCR runs locally. No Textract, no Rekognition, no paid API, no network
# call at inference time.
#
# EasyOCR (default engine). It requires torch; install the CPU-only wheel
# to avoid pulling ~2 GB of CUDA libraries you cannot use in a container
# with no GPU:
#
#   pip install torch==2.4.1 torchvision==0.19.1 \
#       --index-url https://download.pytorch.org/whl/cpu
#   pip install easyocr==1.7.2
#
# Tesseract (lightweight alternative, OCR_ENGINE=tesseract). Needs the
# system binary as well as the Python wrapper:
#   Linux:  apt install tesseract-ocr
#   macOS:  brew install tesseract
boto3==1.34.142
botocore==1.34.142
Pillow==10.4.0
pytesseract==0.3.13
easyocr==1.7.2
